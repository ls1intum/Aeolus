api: v0.0.1
metadata:
  name: windfile
  description: |
    This is a simple example of a windfile that passes validation.
  author:
    name: Andreas Resch
  gitCredentials: artemis_gitlab_admin_credentials
repositories:
  tests:
    url: https://github.com/ls1intum/Aeolus.git
    branch: develop
    path: .
actions:
  - name: what-are-these-files
    script: |
      ls -la
  - name: run
    script: |
      echo "Hello World"
